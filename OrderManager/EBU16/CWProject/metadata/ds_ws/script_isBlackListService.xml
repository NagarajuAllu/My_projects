<?xml version="1.0" encoding="UTF-8" ?>
<script name="ds_ws.isBlackListService">
  <label>Is BlackList Service</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="serviceNumber" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var isInBlackList = false;

    var searchDocument = new Document("ds_ws:STC_BLACKLIST_SERVICE");
    searchDocument.serviceNumber = serviceNumber;

    var resultList = Finder.runFinder("ds_ws:findBlackListService", "select", searchDocument);
    if(resultList != null && resultList.length > 0) {
        isInBlackList = true;
    }

    return isInBlackList;
  ]]></script>
</script>