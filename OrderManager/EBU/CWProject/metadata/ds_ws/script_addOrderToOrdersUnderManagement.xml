<?xml version="1.0" encoding="UTF-8" ?>
<script filenameHash="e2fb3b21b9e4db9e9f20c1907b209068" versionHash="a87ff679a2f3e71d9181a67b7542122c" name="ds_ws.addOrderToOrdersUnderManagement">
  <label>Add Order To OrdersUnderManagement</label>
  <metaVersion>4</metaVersion>
  <parameterList>
    <parameter name="orderNumber" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var searchDoc = new Document("ds_ws.orderUnderManagementDoc");
    searchDoc.orderNumber = orderNumber;

    var resultList = Finder.runFinder("ds_ws.findOrderUnderManagement", "select", searchDoc);

    if(resultList != null && resultList.length > 0) {
        Global.throwException("AE0018", orderNumber);
    }

    Global.beginTransaction("ORDER");
    searchDoc.save();
    Global.commitTransaction("ORDER");
  ]]></script>
</script>