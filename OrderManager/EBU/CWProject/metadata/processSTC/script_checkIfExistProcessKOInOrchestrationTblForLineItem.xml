<?xml version="1.0" encoding="UTF-8" ?>
<script filenameHash="dc2f77c69e9d9b30e3549ff27cb86df0" versionHash="a87ff679a2f3e71d9181a67b7542122c" name="processSTC.checkIfExistProcessKOInOrchestrationTblForLineItem">
  <label>Check if Exists Process KO in Orchestration Table For LineItem</label>
  <metaVersion>4</metaVersion>
  <parameterList>
    <parameter name="cwOrderId" type="rifp">
      <type>dtype_cwf.docId</type>
    </parameter>
    <parameter name="lineItemId" type="rifp">
      <type>dtype_cwf.docId</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var inputDoc = new Document("processSTC.STC_ORDER_ORCHESTRATION");
    inputDoc.cwOrderId = cwOrderId;
    inputDoc.cwDocId = lineItemId;
    inputDoc.provisioningProcessResult = "KO";

    var orchestrationRecordList = Finder.runFinder("processSTC.findSTC_ORDER_ORCHESTRATION", "select", inputDoc);

    var existProcessKO = false;
    if(orchestrationRecordList != null && orchestrationRecordList.length > 0) {
        existProcessKO = true;
    }

    debugPrintln("existProcessKOInOrchestrationTable for OrderId " + cwOrderId + " & LineItemId " + lineItemId + " = " + existProcessKO);
    return existProcessKO;
  ]]></script>
</script>