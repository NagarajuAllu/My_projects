<?xml version="1.0" encoding="UTF-8" ?>
<script name="processSTC.deleteGraniteResponseFromDB">
  <label>Delete Granite Response From DB</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="processId" type="rifp">
      <type>dtype_cwf.processId</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var inputDoc = new Document("processSTC.STC_GRANITE_RESPONSE");
    inputDoc.processId = processId;
    inputDoc.cwdocid = null;

    var graniteResponseDS = null;
    var graniteResponseFound = Finder.runFinder("processSTC.findSTCGraniteResponse", "select", inputDoc);
    if(graniteResponseFound != null && graniteResponseFound.length > 0) {
        graniteResponseFound[0].deleteItem();
    }
  ]]></script>
</script>