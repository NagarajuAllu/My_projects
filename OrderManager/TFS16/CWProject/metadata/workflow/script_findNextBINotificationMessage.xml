<?xml version="1.0" encoding="UTF-8" ?>
<script name="workflow.findNextBINotificationMessage">
  <label>Find Next BI Notification Message</label>
  <metaVersion>25</metaVersion>
  <script><![CDATA[
    var resultList = Finder.runFinder("workflow.findNextBINotificationRecords", "select", null);

    var nextBINotificationMsg = null;
    if(resultList != null && resultList.length > 0) {
        nextBINotificationMsg = resultList[0];
    }

    return nextBINotificationMsg;
  ]]></script>
</script>