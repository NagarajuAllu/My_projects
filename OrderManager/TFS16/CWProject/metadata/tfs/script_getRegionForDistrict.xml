<?xml version="1.0" encoding="UTF-8" ?>
<script name="tfs.getRegionForDistrict">
  <label>Get Region For District</label>
  <metaVersion>4</metaVersion>
  <parameterList>
    <parameter name="districtName" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var finder = new Finder("configuration.cityDistrictRegionFinder");

    finder.searchDocument.districtName = districtName;
    var result = finder.search();

    if(result && result.length > 0){
        return result[0].regionCode;
    }

    return null;

    //return "001";
  ]]></script>
</script>