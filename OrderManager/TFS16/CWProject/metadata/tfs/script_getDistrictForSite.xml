<?xml version="1.0" encoding="UTF-8" ?>
<script name="tfs.getDistrictForSite">
  <label>Get District For Site</label>
  <metaVersion>4</metaVersion>
  <parameterList>
    <parameter name="siteA" type="rifp">
      <type>dtype_granite.siteReference</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var finder = new Finder("configuration.cityDistrictRegionFinder");

    finder.searchDocument.cityName = siteA.CITY;

    var result = finder.search();
    if(result && result.length > 0){
        return result[0].districtName;
    }
    return null;
    //return "District for site " + siteA.SITE_HUM_ID;
  ]]></script>
</script>