<?xml version="1.0" encoding="UTF-8" ?>
<script filenameHash="66179eb1054ed9ae8fc720406cc42ed" versionHash="eccbc87e4b5ce2fe28308fd9f2a7baf3" name="stcw.map_submitOrderResp_orderAck">
  <guid>{0cf5ef19-29aa-d011-72cd-c6b16b2b1e0f}</guid>
  <label>(map) map_submitOrderResp_orderAck</label>
  <metaVersion>3</metaVersion>
  <parameterList>
    <parameter name="output" type="rifp">
      <mandatory>true</mandatory>
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
    <parameter name="input" type="rifp">
      <mandatory>true</mandatory>
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
  </parameterList>
  <valueType>dtype_com.conceptwave.system.Boolean</valueType>
  <script><![CDATA[
    if (output == undefined || output == null || output.metadataType != "siordAck:processRequest_el") {
          Global.logDebug ("map_submitOrderResp_orderAck - nonexistant/wrong output: " + output);
          return (false);
    }

    if (input == undefined || input == null || input.metadataType != "grws:submitOrderResponse") {
          Global.logDebug ("map_submitOrderResp_orderAck - nonexistant/wrong input: " + input);
          return (false);
    }

    var outputRequest = output.Request;
    var inputResult = input.result;
    var inputResultStatus = input.result.Status;

    outputRequest.AssetNumber      = inputResult.AssetNumber;
    outputRequest.AssetStatus      = inputResult.AssetStatus;
    outputRequest.BusinessUnit     = inputResult.BusinessUnit;
    outputRequest.OrderNumber      = inputResult.OrderNumber;
    outputRequest.WorkOrderStatus  = inputResult.WorkOrderStatus;

    outputRequest.ErrorCode        = inputResultStatus.ErrorCode;
    outputRequest.ErrorDescription = stcc.limitStringLengthToCRMValue(inputResultStatus.ErrorDescription);
    outputRequest.ErrorStatus      = inputResultStatus.Status;
    outputRequest.ErrorType        = inputResultStatus.ErrorType;
    outputRequest.SourceErrorCode  = inputResultStatus.SourceErrorCode;

    return (true);
  ]]></script>
</script>