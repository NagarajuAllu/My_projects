<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.isCollocationServiceType">
  <label>Is Collocation ServiceType</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="serviceType" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var inputDoc = new Document("stcconf.STCW_COLLOCATION_SERVICETYPE");
    inputDoc.serviceType = serviceType;

    var isInList = false;

    var resultList = Finder.runFinder("stcconf.findCollocationServiceTypes", "select", inputDoc);
    if(resultList != null && resultList.length > 0) {
        isInList = true;
    }

    return isInList;
  ]]></script>
</script>