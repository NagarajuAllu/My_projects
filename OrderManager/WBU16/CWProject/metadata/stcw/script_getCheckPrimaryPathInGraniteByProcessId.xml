<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.getCheckPrimaryPathInGraniteByProcessId">
  <label>Get CheckPrimaryPathInGranite By ProcessId</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="processId" type="rifp">
      <type>dtype_cwf.docId</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var searchDoc = new Document("stcc.genericSearchDocWithId");
    searchDoc.docId = processId;

    var resultList = Finder.runFinder("stcw.findCheckPrimaryPathInGraniteByProcessId", "select", searchDoc);
    var checkPathInGraniteDoc = null;
    if(resultList != null && resultList.length > 0) {
        checkPathInGraniteDoc = resultList[0];
    }
    return checkPathInGraniteDoc;
  ]]></script>
</script>