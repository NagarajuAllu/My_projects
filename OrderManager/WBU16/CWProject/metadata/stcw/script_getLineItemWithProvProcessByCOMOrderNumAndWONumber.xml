<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.getLineItemWithProvProcessByCOMOrderNumAndWONumber">
  <label>Get LineItem By COMOrderNumber &amp; WorkOrderNumber</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="comOrderNumber" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
    <parameter name="workOrderNumber" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var inputDoc = new Document("stcw.search_LineItem_ByCOM_WorkOrderNum");
    inputDoc.comOrderNumber = comOrderNumber;
    inputDoc.workOrderNumber = workOrderNumber;

    var resultList = Finder.runFinder("stcw.findLineItemWithProvProcessByCOMOrderNum_WONumber", "select", inputDoc);

    var lineItem = null;

    if(resultList != null && resultList.length > 0) {
        lineItem = resultList[0];
    }
    return lineItem;
  ]]></script>
</script>