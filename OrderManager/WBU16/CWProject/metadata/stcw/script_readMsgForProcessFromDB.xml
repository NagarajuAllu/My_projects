<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.readMsgForProcessFromDB">
  <label>Read Message From DB</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="processId" type="rifp">
      <type>dtype_cwf.processId</type>
    </parameter>
    <parameter name="dataStructureType" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
    <parameter name="msgType" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
    <parameter name="docId" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var inputDoc = new Document("stcw.STCW_GRANITE_EVENT_STORAGE");
    inputDoc.processId = processId;
    inputDoc.msgType = msgType;
    if(docId == null) {
        inputDoc.cwDocId = null;
    }
    else {
        inputDoc.cwDocId = docId;
    }

    var graniteResponseDS = null;
    var graniteResponseFound = Finder.runFinder("stcw.findSTCW_GRANITE_EVENT_STORAGE", "select", inputDoc);
    if(graniteResponseFound != null && graniteResponseFound.length > 0) {
        var xmlMessage = graniteResponseFound[0].graniteMsg;
        var convertedList = Global.getItemsFromXML(xmlMessage, dataStructureType);
        if (convertedList != null && convertedList.length > 0) {
            graniteResponseDS = convertedList[0];
        }

    }
    return graniteResponseDS;
  ]]></script>
</script>