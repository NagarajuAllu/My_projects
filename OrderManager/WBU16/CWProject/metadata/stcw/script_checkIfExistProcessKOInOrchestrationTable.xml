<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.checkIfExistProcessKOInOrchestrationTable">
  <label>Check if Exists Process KO in Orchestration Table</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="cwOrderId" type="rifp">
      <type>dtype_cwf.docId</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var inputDoc = new Document("stcw.STCW_ORDER_ORCHESTRATION");
    inputDoc.cwOrderId = cwOrderId;
    inputDoc.provisioningProcessResult = "KO";

    var orchestrationRecordList = Finder.runFinder("stcw.findSTCW_ORDER_ORCHESTRATION", "select", inputDoc);

    var existProcessKO = false;
    if(orchestrationRecordList != null && orchestrationRecordList.length > 0) {
        existProcessKO = true;
    }

    debugPrintln(stcc.getSysdateForLog() + " existProcessKOInOrchestrationTable for Order " + cwOrderId + " = " + existProcessKO);
    return existProcessKO;
  ]]></script>
</script>