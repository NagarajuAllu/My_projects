<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.compareReceivedSubServBlockParamsWithExistingOne">
  <label>Compare Received SubService Block Parameters With Existing One</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="existingLineItemContainer" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
    <parameter name="receivedLineItemContainer" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
    <parameter name="validationErrors" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
    <parameter name="lineItemIdentifier" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var debugMsg = "compareReceivedSubServBlockParamsWithExistingOne (" + lineItemIdentifier + ")";

    // nv check; it loops in existing NV pair and verifies if it exists also in received one
    if(existingLineItemContainer.subServiceLineItemBlockParameters != null) {
        var missingReceived = 0;
        for(var i=0; i<existingLineItemContainer.subServiceLineItemBlockParameters.length; i++) {
            var existingNVName = existingLineItemContainer.subServiceLineItemBlockParameters[i].subServiceLineItemBlockParameter.name;
            var found = false;
            var foundWithError = false;
            var lastErrorFound = null;
            // search nv in received
            if(receivedLineItemContainer.subServiceLineItemBlockParameters != null) {
                for(var j=0; j<receivedLineItemContainer.subServiceLineItemBlockParameters.length && !found; j++) {
                    if(existingNVName == receivedLineItemContainer.subServiceLineItemBlockParameters[j].subServiceLineItemBlockParameter.name) {
                        // found; comparing the values
                        found = true;
                        var receivedValues = stcw.getBlockValuesAsString(receivedLineItemContainer.subServiceLineItemBlockParameters[j].subServiceLineItemBlockParameter.id, ";");
                        var existingValues = stcw.getBlockValuesAsString(existingLineItemContainer.subServiceLineItemBlockParameters[i].subServiceLineItemBlockParameter.id, ";");
                        var resultArray = compareValues_(receivedValues, existingValues, existingNVName, "BlockParameter");
                        if(resultArray != null) {
                            found = false;
                            foundWithError = true;
                            lastErrorFound = resultArray;
                        }
                        else {
                            foundWithError = false;
                            lastErrorFound = null;
                        }
                    }
                }
            }

            if(!found) {
                if(foundWithError) {
                    // found but it was wrong! adding error
                    validationErrors = stcw.appendToValidationErrors(lastErrorFound[0], lastErrorFound[1], validationErrors, debugMsg);
                }
                else {
                    // not found NV; adding error
                    validationErrors = stcw.appendToValidationErrors("AE0065", Global.translateText("AE0065", null, ["BlockParameter", existingNVName, "SubServiceLineItem", lineItemIdentifier, "received", "existing"]), validationErrors);
                    missingReceived++;
                }
            }
        }

        if(receivedLineItemContainer.subServiceLineItemBlockParameters != null) {
            if((receivedLineItemContainer.subServiceLineItemBlockParameters.length + missingReceived) != existingLineItemContainer.subServiceLineItemBlockParameters.length) {
                // it means that there are additional attributes in received that are not in existing! ERROR!
                validationErrors = stcw.appendToValidationErrors("AE0066", Global.translateText("AE0066", null, ["SubServiceLineItem", lineItemIdentifier, "BlockParameters"]), validationErrors);
            }
        }
    }
    else {
        if(receivedLineItemContainer.subServiceLineItemBlockParameters != null) {
            // it means that there are additional attributes in received that are not in existing! ERROR!
            validationErrors = stcw.appendToValidationErrors("AE0066", Global.translateText("AE0066", null, ["SubServiceLineItem", lineItemIdentifier, "BlockParameters"]), validationErrors);
        }
    }

    return validationErrors;

    function compareValues_(receivedAttribute, existingAttribute, attributeName, category) {
        var resultArray = null;

        if(receivedAttribute == null) {
            if(existingAttribute != null) {
                resultArray = ["AE0055", Global.translateText("AE0055", null, [category, attributeName, "existing"])];
            }
        }
        else {
            if(existingAttribute == null) {
                resultArray = ["AE0057", Global.translateText("AE0057", null, [category, attributeName, "existing"])];
            }
            else {
                if(! receivedAttribute.toString().equals(existingAttribute.toString())) {
                    resultArray = ["AE0056", Global.translateText("AE0056", null, [category, attributeName, receivedAttribute, existingAttribute, "existing"])];
                }
            }
        }

        return resultArray;
    }
  ]]></script>
</script>