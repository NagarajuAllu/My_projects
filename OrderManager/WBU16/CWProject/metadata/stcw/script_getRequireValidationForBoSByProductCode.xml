<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.getRequireValidationForBoSByProductCode">
  <label>Get Require Validation For BoS by Product Code</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="productCode" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var requireValidationForBoS = false;

    var searchDoc = new Document("stcconf.search_ProductCodeDoc");
    searchDoc.productCode = productCode;

    var resultList = Finder.runFinder("stcconf.findAllProductCodes", "select", searchDoc);

    if(resultList != null && resultList.length > 0) {
        requireValidationForBoS = resultList[0].requireValidationForBoS;
    }

    return requireValidationForBoS;
  ]]></script>
</script>