<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.getLineItemByWorkOrderNumberAndProvFlag">
  <label>Get LineItem By WorkOrderNumber &amp; ProvisioningFlag</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="workOrderNumber" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
    <parameter name="provisioningFlag" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var searchDoc = new Document("stcw.search_LineItem");
    searchDoc.workOrderNumber = workOrderNumber;
    searchDoc.provisioningFlag = provisioningFlag;

    var resultList = Finder.runFinder("stcw.findLineItemByWorkOrderNumberAndProvFlag", "select", searchDoc);
    var activeLI = null;
    if(resultList != null && resultList.length > 0) {
        activeLI = resultList[0];
    }
    return activeLI;
  ]]></script>
</script>