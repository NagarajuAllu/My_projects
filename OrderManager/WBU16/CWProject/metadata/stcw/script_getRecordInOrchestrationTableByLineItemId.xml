<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.getRecordInOrchestrationTableByLineItemId">
  <label>Get Record In Orchestration Table By LineItemId</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="docId" type="rifp">
      <type>dtype_cwf.docId</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var inputDoc = new Document("stcw.STCW_ORDER_ORCHESTRATION");
    inputDoc.cwDocId = docId;

    var orchestrationRecord = null;

    var resultList = Finder.runFinder("stcw.findSTCW_ORDER_ORCHESTRATION", "select", inputDoc);

    if(resultList != null && resultList.length > 0) {
        orchestrationRecord = resultList[0];
    }

    return orchestrationRecord;
  ]]></script>
</script>