<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.checkIfLineItemIsFeasibleForQuote">
  <label>Check If LineItem is Feasible For Quote</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="elementStatusInUpperCase" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
    <parameter name="orderType" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var completed = false;
    if(orderType == "F") {
        // Old: (before GRAN-393 and CR 14039/14040)
        // completed = (elementStatusInUpperCase == "FEASIBLE");
        // New:
        completed = (elementStatusInUpperCase == "FEASIBLE" || elementStatusInUpperCase == "PLANNED FEASIBLE");

    }

    return completed;
  ]]></script>
</script>