<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.performValidationOfMandatoryAttributesInOrdHeader">
  <label>Perform Validation Of Mandatory Attributes In OrderHeader</label>
  <metaVersion>4</metaVersion>
  <parameterList>
    <parameter name="orderHeader" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
    <parameter name="isSubmit" type="rifp">
      <type>dtype_com.conceptwave.system.Boolean</type>
    </parameter>
    <parameter name="failureArray" type="rifp">
      <type>nmeta_com.conceptwave.system.Object</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var mandatoryAttributes = stcw.getOrderHeaderMandatoryAttribsByOrderType(orderHeader.orderType);

    var validationErrors = null;

    if(mandatoryAttributes != null) {
        var foundWithValue = false;
        for(var i=0; i<mandatoryAttributes.length; i++) {
            foundWithValue = false;

            var attributeValue = orderHeader[mandatoryAttributes[i]];

            if(!stcc.stringHasValue(attributeValue)) {
                if(validationErrors == null) {
                    validationErrors = new Array();
                }

                // AE0039: Missing Mandatory Attribute: {0}
                validationErrors.push("AE0039");
                validationErrors.push(Global.translateText("AE0039", null, [mandatoryAttributes[i]]));
            }
        }
    }

    if(validationErrors != null) {
    debugPrintln(stcc.getSysdateForLog() + " performValidationOfMandatoryAttributesInOrdHeader (" +orderHeader.orderNumber + "): Found errors [" + validationErrors + "]");
        failureArray = stcw.addValidationErrorsToFailureArrayForSub_UpdInWD(validationErrors, orderHeader.orderNumber, failureArray, isSubmit);
    }

    return failureArray;
  ]]></script>
</script>