<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.getGraniteServiceTypeForSOMInternalName">
  <label>Get Granite ServiceType For SOM Internal Name</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="somInternalName" type="rifp">
      <type>dtype_stcw.string25</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var result = null;

    if(stcc.stringHasValue(somInternalName)) {
        var convertValueFinder = new Finder ("stcconf.findGraniteServiceTypeForSOMName");
        convertValueFinder.searchDocument.serviceType = somInternalName;
        convertValueFinder.search();

        var resultList = convertValueFinder.list;

        if (resultList != null && resultList.size > 0) {
            result = resultList.start.giServiceType;
        }
    }

    return result;
  ]]></script>
</script>