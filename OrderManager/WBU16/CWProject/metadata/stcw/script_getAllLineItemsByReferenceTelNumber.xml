<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.getAllLineItemsByReferenceTelNumber">
  <label>Get All LineItems By ReferenceTelNumber</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="referenceTelNumber" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var resultList = null;

    if(referenceTelNumber != null) {
        var searchDoc = new Document("stcw.search_LineItem");
        searchDoc.referenceTelNumber = referenceTelNumber;

        resultList = Finder.runFinder("stcw.findLineItem", "select", searchDoc);
    }
    return resultList;
  ]]></script>
</script>