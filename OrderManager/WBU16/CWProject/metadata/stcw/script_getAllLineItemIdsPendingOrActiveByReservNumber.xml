<?xml version="1.0" encoding="UTF-8" ?>
<script name="stcw.getAllLineItemIdsPendingOrActiveByReservNumber">
  <label>Get All LineItem Identifiers Pending or Active By ReservationNumber</label>
  <metaVersion>25</metaVersion>
  <parameterList>
    <parameter name="reservationNumber" type="rifp">
      <type>dtype_com.conceptwave.system.String</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var resultList = null;

    if(reservationNumber != null) {
        var searchDoc = new Document("stcw.search_LineItem");
        searchDoc.reservationNumber = reservationNumber;

        resultList = Finder.runFinder("stcw.findLineItemIdActiveOrPendingByReservationNumber", "select", searchDoc);
    }
    return resultList;
  ]]></script>
</script>