<?xml version="1.0" encoding="UTF-8" ?>
<script name="granite.getSite">
  <label>Get Site</label>
  <metaVersion>4</metaVersion>
  <parameterList>
    <parameter name="siteInstId" type="rifp">
      <type>dtype_granite.INST_ID_16</type>
    </parameter>
  </parameterList>
  <script><![CDATA[
    var site = null;

    if(siteInstId != null) {
        var siteFinder = new Finder("granite.siteFinder");
        siteFinder.searchDocument.INST_ID = siteInstId;
        siteFinder.searchDocument.SITE_HUM_ID = null;

        var siteList = siteFinder.search();
        if(siteList != null && siteList.size > 0) {
            site = siteList[0];
        }
    }

    return site;
  ]]></script>
</script>