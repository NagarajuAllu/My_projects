<?xml version="1.0" encoding="UTF-8" ?>
<script filenameHash="9df89ce60ce17fd4fec435b6fad3e01b" versionHash="eccbc87e4b5ce2fe28308fd9f2a7baf3" name="migrationSTC.searchAllOrderEntryExpediterOrders">
  <guid>{057fddd8-b923-aa92-5124-aa4f11cf4da1}</guid>
  <highlight>36</highlight>
  <label>Search All Order Entry (Expediter Orders) [Through Global.runSQLQuery(...)]</label>
  <metaVersion>3</metaVersion>
  <script><![CDATA[
    var debug = "[SEARCH ALL ORDER ENTRY (EXPEDITER ORDERS)] ";
    var result = null;

    // Definition of the SQL script to be executed
    var sql = "select distinct * from STC_EXPEDITOR v";
    var output = Global.runSQLQuery("XNG PRODUCTION [USER:STC_TARGET2]",
                                                  "migrationSTC:STC_EXPEDITOR",
                                                  99999,
                                                  null,
                                                  sql);

    if (output != null && output.size > 0) {
    debugPrintln("[" + ds_ws.common_currentDateAsStringDDMMYYYYHH24MISS() + "]" + debug + " RETRIEVED # " + output.size + " RECORDS FROM STC_EXPEDITOR VIEW...")
                result = new Array();

     //           var ratio = output.totalSize / output.size;
     //           for (var j = 0; j < ratio; j++) {
                      // [A]. Increase the page load
                      output.loadPage(0);
                      // [B]. Fetch the current page
                      for (var i = 0; i < output.size; i++){
                            result = result.concat(output[i]);
                      }
     //           }
          }
          else{
    debugPrintln("[" + ds_ws.common_currentDateAsStringDDMMYYYYHH24MISS() + "]" + debug + " RETRIEVED NO RECORDS FROM STC_EXPEDITOR VIEW...")
          }

          return result;
  ]]></script>
</script>